<div class="container">
  <div class="alert alert-info">
    <h1><%= @post.title%></h1>
  </div>
  <div class="alert alert-info">
    <p><%= @post.content%></p>
    <div class="alert alert-info">
      <h2 id="com2header">Comments</h2>
        <% @post.comments.each do |c| %>
        <div class="alert alert-info"><%= c.content %></div>
          <br>
        <% end %>
        <%= link_to "Comment", new_post_comment_path(@post) %>

      </div>

    <% if current_user %>
      <% if @post.user == current_user %>
        <p class="text-right">
          <%= link_to "Delete this post", post_path(@post), method: :delete, data: {confirm: "Are you sure?"} %>
        </p>
      <% end %>
    <% end %>

    <% if current_user %>
  <% if @comment.user_id == current_user.id %>
  <%= link_to "Delete", post_comment_path(@post, comment), method: :delete, data: { confirm: "Are you sure you want to delete this?"} %> <br>
  <% end %>
<% end %>

</div>
</div>
